use_default_settings: true

general:
  debug: false
  instance_name: "AI-Q SearXNG"

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "aiq-local-searxng-change-me" # pragma: allowlist secret
  limiter: false
  image_proxy: false

# Added for SniperIP discovery reuse: marketplace + reverse-image engines
engines:
  - name: ebay
    inactive: false
    disabled: false
  - name: tineye
    disabled: false

--- a/src/engines/searx.rs
+++ b/src/engines/searx.rs
@@ -59,8 +59,12 @@
         // * https://piped.video/watch?v=bVJ-mWWL7cE
         safe_search = u8::from(safe_search >= 3) * 2;

+        let base_url = std::env::var("WEBSURFX_SEARX_URL")
+            .unwrap_or_else(|_| "https://searx.be".to_string())
+            .trim_end_matches('/')
+            .to_string();
         let url: String = format!(
-            "https://searx.be/search?q={query}&pageno={}&safesearch={safe_search}",
+            "{base_url}/search?q={query}&pageno={}&safesearch={safe_search}",
             page + 1
         );
